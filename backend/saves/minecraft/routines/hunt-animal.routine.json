{
  "name": "hunt-animal",
  "description": "指定した動物を探して近づき、連続攻撃で倒してドロップを拾う。cow/pig/sheep を推奨。【combat は追跡が遅く非効率。このルーチンは find → move-to → attack-continuously で確実に倒す】",
  "params": [
    {
      "name": "animalType",
      "type": "string",
      "description": "狩る動物の種類 (cow, pig, sheep 推奨。chicken は小さくて当たりにくい)",
      "required": false,
      "default": "cow"
    },
    {
      "name": "searchRadius",
      "type": "number",
      "description": "探索半径",
      "required": false,
      "default": 64
    }
  ],
  "steps": [
    {
      "skill": "find-nearest-entity",
      "args": {
        "entityType": "${animalType}",
        "maxDistance": "${searchRadius}"
      },
      "outputVar": "findResult",
      "onFailure": "abort"
    },
    {
      "skill": "attack-continuously",
      "args": {
        "entityName": "${animalType}",
        "maxAttacks": 20
      },
      "onFailure": "continue"
    },
    {
      "skill": "wait-time",
      "args": { "milliseconds": 1500 },
      "onFailure": "skip"
    },
    {
      "skill": "pickup-nearest-item",
      "args": { "maxDistance": 16 },
      "onFailure": "skip"
    },
    {
      "skill": "pickup-nearest-item",
      "args": { "maxDistance": 16 },
      "onFailure": "skip"
    }
  ],
  "failureEscalation": "system2",
  "source": "manual",
  "stats": { "runs": 0, "successes": 0, "failures": 0, "avgDurationMs": 0 }
}
